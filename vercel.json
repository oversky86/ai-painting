{
  "buildCommand": "(npx prisma db push --accept-data-loss || echo 'WARNING: prisma db push failed, skipping') && npx react-router build",
  "installCommand": "npm install && npx prisma generate",
  "framework": "react-router",
  "env": {
    "NODE_ENV": "production"
  }
}
